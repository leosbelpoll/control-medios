{% extends 'base.html.twig' %}

{% block body %}
    <h1>Atributo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ atributo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ atributo.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ atributo.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('atributo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('atributo_edit', { 'id': atributo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
