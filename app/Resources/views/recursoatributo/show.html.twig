{% extends 'base.html.twig' %}

{% block body %}
    <h1>Recursoatributo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recursoAtributo.id }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ recursoAtributo.valor }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recursoatributo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('recursoatributo_edit', { 'id': recursoAtributo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
